<%= form_with(url:search_pokemons_path, method: :get, local: true) do %>
  <%= text_field_tag(:keywords, nil, class: "input", placeholder: "Search for Pokemons") %>
  <%= submit_tag("Search!", class: "button is-primary mt-1") %>
<% end %>